{{define "body"}}

 <h1> hackNY Fellows Class of 2013 </h1>

{{range . }}

	<div class="fellow">
	    <img class="headshot" src="{{.HeadshotUrl}}"></img>
	    <div class="name"><a href="{{.Blog}}">{{.Name}}</a></div>
	    <img class="startup-logo" src="{{.StartupLogo}}"></img>
	</div>

{{end}}

{{end}}
